apiVersion: v1
kind: Service
metadata:
  name: flask-chat-server
spec:
  clusterIP: None        # headless
  selector:
    app: flask-chat-server
  ports:
  - name: http
    port: 5050
    targetPort: 5050
